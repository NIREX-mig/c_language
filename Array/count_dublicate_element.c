#include < stdio.h>

int main()
{
    // initialise variable
    int arr[30];
    int n;

    // input array size
    printf("Enter the size of array");
    scanf("%d", &n);

    // Enter the size
    printf("Enter the element\n");
    for (int i = 0; i < n; i++)
    {
        scanf("%d",&arr[i]);
    }

    // count dublicate element 
    for (int i = 0; i < n; i++)
    {
        
    }
    

        return 0;
}